# 🌎 RoscomVPN GeoIP

### Генерирует самый актуальный `geoip.dat` для **Xray-core** и **V2Ray-core**, включающий **IP-адреса: стран (RU/BY/KZ) и IT-гигантов (включая их зарубежные активы) - аффилированных с правительством РФ**

#### Добавлены диапазоны IP-адресов:
- MaxMind GeoIP (всегда свежий список всех RU/BY/KZ диапазонов IP)
- Все известные (включая зарубежные) от **VK Company** (VK, Mail.Ru, OK, My.Games и т.д) - категория `direct`
- Все известные (включая зарубежные) от **Yandex** (Яндекс, Yandex.Cloud, Yandex.Disk и т.д) - категория `direct`

#### Изменения и исключения:
- Удалены неиспользуемые категории из основной репы V2Ray (минимизация потребления RAM)
- 4pda (исключен из `direct`, хоть и являясь частью ру-сегмента - какое-то кол-во контента в РФ на 4pda недоступно)

## 📥 **Статические ссылки на latest версию**  
https://github.com/hydraponique/roscomvpn-geoip/releases/latest/download/geoip.dat

https://cdn.jsdelivr.net/gh/hydraponique/roscomvpn-geoip@release/geoip.dat

## 📅 Обновления
Файл **обновляется каждый четверг** и **при внесении любого изменения в данный репозиторий**

## 🛠 Использование с Xray/V2Ray
Добавьте правило `geoip:direct` в конфигурацию Xray/V2Ray, чтобы **направлять НУЖНЫЙ трафик напрямую**:

```json
{
  "routing": {
    "rules": [
      {
        "type": "field",
        "ip": [
          "geoip:direct"
        ],
        "outboundTag": "direct"
      }
    ]
  }
}
```
